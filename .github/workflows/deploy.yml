name: Deploy Homelab i5

on:
  push:
    branches: [ main ]

jobs:
  test:
    name: Testes de Sanidade (Frontend e Backend)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Rodar Testes Frontend
        run: |
          npm install
          npm test

      - name: Rodar Testes Backend
        run: |
          cd server
          npm install
          npm test sanity.test.js # Roda apenas a sanidade nesta etapa

  ai-validation:
    name: Validação da IA
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Instalar Backend
        run: |
          cd server
          npm install

      - name: Validar Gemini API
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          cd server
          npm test ai.test.js

  deploy:
    name: Deploy para Homelab
    runs-on: ubuntu-latest
    needs: ai-validation
    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4

      - name: Conectar na Rede Tailscale
        uses: tailscale/github-action@v2
        with:
          authkey: ${{ secrets.TAILSCALE_AUTHKEY }}
          ephemeral: true

      - name: Executar Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: dyone
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            cd /home/dyone/controlefinanceiro
            git fetch origin main
            git reset --hard origin/main
            docker compose up -d --build
            docker image prune -f
